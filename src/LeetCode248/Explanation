三道中心对称数问题的解析都写在这里
1.246判断是否是中心对称数是最简单的一道，诀窍在于利用哈希表来存储对称对（6和9互为对称对），当左侧数不在哈希表中或者右侧数与左侧数不成对称对时，则不为对称数
2.247求给定位数的所有中心对称数，用递归和迭代都可以解决，迭代需要两个List分别存储奇/偶位数的结果，递归则不用。关键点在于处理偶数情况下以0开头的字符串，这个时候就需要判断当前n是否是要求的n，如果不是，则加入其中，因为对后续有用；如果是，则不加入，因为在最终答案中，以0开头的字符串是无效答案（除非该字符串本身就是0）
3.248是最复杂的一道，求给定范围内的所有中心对称数个数，有非常多难处理的点，一一分析如下
    1)首先与上一题不同的是，这道题目要把位数从1到high的长度（必须要判别一下low的位数是否<=high的位数，否则直接返回0）的所有中心对称数都求出来并保存
    2)由于要统计不同位数的中心对称数的个数，所以在求解过程中不用去除0开头的字符串，而是用一个counts表记住每个位数对应的中心对称数个数（上一层未去除0开头字符串的总个数*4）
    3)同样由于未去除0开头的字符串，所以在比较时有效cur的取值应该是要么就是0，要么不能以0开头
    4)只有同样长度的字符串才能用compareTo函数直接比较求得对应数值的大小，所以low与high长度一致/不一致要分开讨论