1.这道题目是昨天蚂蚁金服OceanBase的视频面代码题，因为比较紧张的原因，我首先想到了最简单的直接排序，但是写的也不全对，因为排序可以到真正查找中位数的时候再进行，可以节约时间
2.然后想到了次优的优化方案，用链表去存储元素，这样查找插入位置时间复杂度是O(N),插入是O(1)，然后寻找中位数的时间复杂度是O(N)，最终时间复杂度就也是O(N)；如果利用容器自带的add函数，那么就是二分查找插入位置是O(logN)，插入移动后续元素是O(N)，所以总的时间复杂度也是O(N)
3.最优的堆解法当时没有想到，本次提交的就是堆解法，要注意的是要始终确保大顶堆中的元素<=小顶堆中的元素，堆中一次插入/删除的时间复杂度是O(logN)