1.用二分查找寻找左右边界关键是当num[mid]==target时的操作，当采用闭区间二分查找时，寻找左边界时right=mid-1；寻找右边界时left=mid+1
2.当左边界寻找结束时，注意边界条件是left == nums.length || nums[left] != target，一旦符合直接返回[-1, -1]，无需再去寻找右边界
3.另：我第一次采用的找到target值再左右线性查找的办法耗时是最短的，但是复杂度并不是最低的，说明测试样例其实构造的不尽合理