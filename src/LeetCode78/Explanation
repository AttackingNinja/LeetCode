1.回溯法的的算法思想：
    a)确定解空间
    b)确定解空间结构（树、图）
    c）以深度优先方式搜索解空间
2.回溯法的递归写法
    backtrack(t){
        if(t>n)
            输出结果并return
        for（当前节点的子节点）{
            记录子节点值
            if（满足约束函数及限界函数）
                backtrack（t+1）
        }
    }
3.new LinkedList<>(result))会用参数中List的值赋给新List