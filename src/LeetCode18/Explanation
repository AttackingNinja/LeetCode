1.应该也可以用双指针做，先固定a和b，然后c和d用双指针从b后一位及最后一位向中间靠近
2.哈希表思想就是记录每个a+b组合的值，在记录的同时去找匹配的c+d（因为a+b=-c-d）
3.双指针的做法类似于三数之和，不同之处在于如何判断如何去重以及可以加速跳出的边界条件
a)主要是第二个索引去重与三数之和不同，第二个索引的开始去重处是j>i+1
b)加速跳出的边界条件是计算第一个所引出可以到达的最小/最大值，最小值大于target直接返回，最大值小于target跳过本次循环
