1.用Arrays.sort()可以对数组排序，对高维数组可以传入一个匿名Comparator对象，然后重写compare方法
2.判断会议冲突本质是判断区间是否有相交部分，对于两个区间[a, b]、[c ,d]，其不相交的条件是a>=d||b<=c，对原数组按照会议开始时间排序后，只需要判断b<=c，因为排序后a<=c<d
3.小顶堆用在本题中模拟会议室集，当数组中会议开始时间在堆顶结束时间之后，即更新堆顶元素；否则插入新的结束时间代表新建会议室